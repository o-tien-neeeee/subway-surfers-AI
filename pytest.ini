[pytest]
testpaths = tests
markers =
    timeout: soft wall-time budget for multi-process tests (enforced when pytest-timeout is installed)
addopts = -ra
filterwarnings =
    ignore::DeprecationWarning
