[pytest]
testpaths = tests
markers =
    timeout: soft wall-time budget for multi-process tests (enforced when pytest-timeout is installed)
    slow: long-running tests (audit-style) — opt-in with `pytest -m slow`
addopts = -ra
filterwarnings =
    ignore::DeprecationWarning
